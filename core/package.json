{
  "name": "@artmapify/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared palette parsing, color matching, and quantization logic for ArtMapify.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"import('node:fs/promises').then(fs => fs.rm('dist', { recursive: true, force: true }))\""
  },
  "dependencies": {
    "pako": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/pako": "^2.0.3",
    "typescript": "^6"
  }
}
